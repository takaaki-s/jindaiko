schema_version: 2
name: bad-v2-action-no-entrypoint
version: 0.1.0
description: v2 manifest action is missing an entrypoint
jin: ">=0.7.0"
install:
  release_asset:
    pattern: "hello-{os}-{arch}"
actions:
  - id: default
