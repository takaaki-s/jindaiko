schema_version: 2
name: bad-v2-action-id
version: 0.1.0
description: v2 manifest has an action id containing uppercase letters
jin: ">=0.7.0"
install:
  release_asset:
    pattern: "hello-{os}-{arch}"
actions:
  - id: Default
    entrypoint: ./bin/hello
