schema_version: 1
name: script-plugin
version: 0.1.0
description: A shell-script plugin that ships its entrypoint directly
jin: ">=0.7.0"
install:
  source:
    entrypoint: ./run.sh
on:
  - status_changed
